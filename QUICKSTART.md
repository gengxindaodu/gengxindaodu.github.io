# 快速开始指南

## 🚀 5分钟快速部署

### 第一步：下载项目

下载并解压 `bible-reading-plan` 文件夹。

### 第二步：上传到GitHub

1. 访问 https://github.com 并登录
2. 点击右上角的 "+" → "New repository"
3. 仓库名称输入: `bible-reading` (或其他名称)
4. 选择 "Public"
5. 点击 "Create repository"

### 第三步：上传文件

**方式1 - 网页上传**（推荐新手）:
1. 在新创建的仓库页面，点击 "uploading an existing file"
2. 将下载的所有文件拖拽到页面
3. 滚动到底部，点击 "Commit changes"

**方式2 - Git命令行**:
```bash
cd bible-reading-plan
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/你的用户名/bible-reading.git
git branch -M main
git push -u origin main
```

### 第四步：启用GitHub Pages

1. 进入仓库的 **Settings**（设置）页面
2. 左侧菜单找到 **Pages**
3. 在 "Source" 下拉菜单选择：
   - Branch: `main`
   - Folder: `/ (root)`
4. 点击 **Save** 按钮
5. 等待1-2分钟，刷新页面会看到网站地址

### 第五步：配置网站

编辑 `_config.yml` 文件：

```yaml
baseurl: "/bible-reading"  # 改成你的仓库名
url: "https://你的用户名.github.io"
```

提交更改后，网站就可以访问了！

## 📝 填充内容

### 1. 生成730天文件模板

运行Python脚本：
```bash
python generate_readings.py
```

这会在 `_daily_readings/` 创建730个文件。

### 2. 添加经文内容

编辑 `_daily_readings/day-XXX.md` 文件，示例：

```markdown
---
day: 1
passage: "创世记 1-2章"
date: 2024-01-01
reflections: ["001-creation"]
---

## 创世记 1章

起初，神创造天地...

[添加完整经文]
```

### 3. 添加读经分享

在 `_reflections/` 创建文件，示例：

```markdown
---
reflection_id: "001-creation"
title: "神的创造彰显祂的大能"
author: "张弟兄"
date: 2024-01-01
passage: "创世记 1-2章"
related_day: 1
---

[分享内容]
```

## 🎯 关键文件说明

```
bible-reading-plan/
├── _config.yml          ⚙️ 网站配置（必须修改）
├── _daily_readings/     📖 每日读经内容
├── _reflections/        💭 读经分享
├── _layouts/            📄 页面布局模板
├── assets/css/          🎨 样式文件
├── index.html           🏠 首页
├── calendar.html        📅 日历页
├── books.html           📚 书卷页
└── README.md            📘 详细文档
```

## 💡 常见问题

### Q: 页面显示404错误？
- 检查GitHub Pages是否已启用
- 确认 `_config.yml` 中的 `baseurl` 正确
- 等待几分钟让GitHub处理

### Q: 如何本地测试？
```bash
bundle install
bundle exec jekyll serve
# 访问 http://localhost:4000
```

### Q: 如何更新内容？
修改文件后：
```bash
git add .
git commit -m "更新内容"
git push
```

### Q: 在哪里获取圣经经文？
- 使用公有版权的和合本圣经
- [Bible API](https://scripture.api.bible/)
- [信望爱圣经工具](https://bible.fhl.net/)
- 手动输入或复制粘贴

## 📖 获取圣经经文的方法

### 方法1: 使用在线圣经
1. 访问 https://www.bible.com 或 https://bible.fhl.net/
2. 查找对应章节
3. 复制经文内容
4. 粘贴到对应的文件中

### 方法2: 使用API（需要编程）
```python
import requests

# 示例：使用Bible API
api_key = "your-api-key"
verse = "GEN.1"
response = requests.get(
    f"https://api.scripture.api.bible/v1/bibles/bible-id/verses/{verse}",
    headers={"api-key": api_key}
)
```

### 方法3: 批量导入
如果有完整的圣经文本文件，可以写脚本批量导入。

## 🎨 自定义外观

修改 `assets/css/style.css` 来改变：
- 颜色主题
- 字体大小
- 布局样式

例如，修改主题色：
```css
/* 将蓝色主题改为绿色 */
.button {
  background-color: #27ae60; /* 原来是 #3498db */
}
```

## 📱 功能扩展

### 添加搜索功能
可以集成 Jekyll Simple Search 或 Algolia

### 添加评论功能
可以集成 Disqus 或 Gitalk

### 添加进度跟踪
使用 LocalStorage 保存用户的阅读进度

### 导出PDF
可以添加将读经内容导出为PDF的功能

## 🤝 寻求帮助

- 查看完整文档: `README.md`
- 部署指南: `DEPLOYMENT.md`
- 在GitHub上提交Issue
- 查看Jekyll官方文档: https://jekyllrb.com

## ✅ 检查清单

在部署前确认：
- [ ] 已创建GitHub仓库
- [ ] 已上传所有文件
- [ ] 已启用GitHub Pages
- [ ] 已修改 `_config.yml` 中的 `baseurl` 和 `url`
- [ ] 已等待几分钟让GitHub处理
- [ ] 可以访问网站地址

## 🎉 下一步

1. 开始填充经文内容
2. 邀请弟兄姊妹贡献读经分享
3. 分享网站链接给更多人
4. 定期更新和维护

---

**祝你顺利完成部署！愿神的话语成为我们生命的粮食！**

如有问题，请查看 `README.md` 获取更多详细信息。
